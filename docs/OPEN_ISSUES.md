# Open Issues

| Issue | Impact | Current Workaround | Required Next Step |
|---|---|---|---|
| No repository access from this build environment (GitHub not in network allowlist) | Code could not be pushed directly to `estisharathamalah/MDEX`; delivered as local files instead | Files packaged for manual push / use with a tool that has GitHub access (e.g. Claude Code with git configured) | Push this tree to the target repo; set up CI (see below) |
| Hole-by-hole / site-by-site 2001 assay, geophysics, and geochemistry data for the pre-OTD-150 decision were not obtained | The single validation experiment uses SYNTHETIC per-site indication strengths rather than real 2001 site-level data | Macro decision context (F1-F6) is real and cited; per-site discriminating evidence is synthetic and clearly tagged | Obtain Ivanhoe/Turquoise Hill NI 43-101 technical reports and any archived pre-2001 exploration data (licensed or archival access) and replace `data/synthetic/candidate_site_indications.json` with a historically sourced equivalent |
| Likelihood tables (evidence -> P(evidence\|hypothesis)) are hand-authored, not fitted | Belief updates are illustrative of the mechanism, not a calibrated geological inference model | Documented as `[ASSUMPTION]` throughout code and configs | Elicit likelihoods from domain-expert geologists or fit against a larger multi-deposit historical corpus |
| Decision-value ranking is single-step-lookahead VOI for information actions, direct EMV for terminal actions | Ranking is defensible but not a full sequential (POMDP) optimum across the whole campaign | Documented as `[ASSUMPTION]` in `decision_engine.py` docstring | Prototype a multi-step lookahead / POMDP formulation as TRL 4 stretch work |
| Only one historical case (Oyu Tolgoi, one decision point) is validated end-to-end | Cannot yet claim broad historical calibration or robustness across deposit types | N/A — explicitly scoped as TRL 3 single-case POC | Add 2-3 additional historical cases (e.g. a documented failure/non-discovery case, to test whether MDEX correctly down-ranks) |
| Economic parameters (drill cost, budget, hypothesis payoffs) are order-of-magnitude ASSUMPTION defaults, not the real 2001 Ivanhoe cost structure | Absolute dollar outputs are illustrative, not a claim about real historical economics | Every parameter tagged with `provenance: ASSUMPTION` in `configs/economics.yaml` | Source real circa-2000-2001 Gobi Desert deep-diamond-drilling cost benchmarks from industry cost databases or company disclosures |
| `additional_geophysical_survey` outcome scenarios in the experiment are non-discriminating across hypotheses (a modeling gap, not a firewall issue) | The experiment reports zero expected uncertainty reduction for the survey action, which understates its realistic information value | Documented here; result is still valid evidence of pipeline mechanics | Redesign survey outcome-to-hypothesis likelihoods so that a survey action can meaningfully update belief |
| No CI pipeline configured yet (no GitHub access from this session) | Tests must be run manually with `python3 tests/test_*.py` (pytest is unavailable in this offline sandbox — install it before running with `pytest` directly in an environment with network access) | Manual test execution confirmed passing (9/9) in this session | Add `.github/workflows/tests.yml` running `pytest tests/` once pushed to a networked environment |
| TRL 4 not claimed as fully met | See `docs/dtv/02_trl_evidence.md` | POC provides partial TRL 4 evidence (integrated pipeline, one full replay) | Multi-case validation, external geologist review, and a second independently reconstructed historical case are the recommended completion criteria |
| Customer/market validation not conducted | Market brief cannot include real customer signals | Market brief explicitly states this gap rather than fabricating LOIs/customers | Conduct real discovery interviews with junior/major mining exploration teams and Saudi mineral sector stakeholders (e.g. via Ma'aden, Saudi Geological Survey contacts) |
